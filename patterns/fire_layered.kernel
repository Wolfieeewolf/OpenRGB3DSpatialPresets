# Name
name: Fire (layered heat)
index: 20
palette: fire

# Kernel
float heat = 0.0;
for(int o = 0; o < 4; o++)
{
    float f = pow(2.0, float(o));
    heat += sin(TWO_PI * (s01 * r * f * 0.31 + ph + tsec * (0.05 + 0.02 * float(o)))) / f;
}
k = clamp(tanh(heat * 1.15), -1.0, 1.0);
