# Name
name: Heartbeat
index: 39

# Kernel
float u = fractf(tsec * 0.8);
if(u < 0.12) k = sin(TWO_PI * u / 0.12);
else if(u < 0.2) k = 0.35 * sin(TWO_PI * (u - 0.12) / 0.08);
else k = -0.55 + 0.2 * sin(TWO_PI * s01 * r);
