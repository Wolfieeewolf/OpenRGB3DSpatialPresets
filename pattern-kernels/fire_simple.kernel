# Name
name: Fire (simple heat)
index: 19
palette: fire

# Kernel
float u = 1.0 - fractf(s01 * r * 0.7 + ph + tsec * 0.07);
float heat = pow(max(0.0, u), 2.5);
heat *= 0.6 + 0.4 * sin(TWO_PI * s01 * r * 2.0);
k = heat * 2.0 - 1.0;
